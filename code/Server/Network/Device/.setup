#pragma once
#include "../../.setup"
#include "../../Common/Sync/.setup"
#include "../../OSI/EUI48/.setup"

StructMemory(Server,Network,Device){
    StructHelp(Server,Common,Sync) scs;
    StructAlign(Server,OSI,EUI48) soe;
    struct list_head node,snal_node;
    struct delayed_work work;
};
